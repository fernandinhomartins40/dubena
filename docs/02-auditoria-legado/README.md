# 02 — AUDITORIA DO LEGADO (referência)

PRDs FIÉIS (linha-a-linha) do sistema legado (ctrl-web original), 15 domínios + índice.
Documentam o comportamento REAL, bugs e segurança verificados no código (arquivo:linha).

São REFERÊNCIA do que o legado faz — base para os PRDs de implementação (01-vigente/IMPL_*).
Não são plano de execução.
